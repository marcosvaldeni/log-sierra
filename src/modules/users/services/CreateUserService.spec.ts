describe('CreateUser', () => {
  it('should be able to create a new user', () => {
    expect(1 + 2).toBe(3);
  });

  it('should not be able to create two appointments on the same', () => {
    expect(1 + 2).toBe(3);
  });
});
